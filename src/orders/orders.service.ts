const order = manager.create(Order, {  userId: user.id,  user: user,  userName: checkoutDto.user_name,  // Fix the property name from userName to user_name  shippingAddress: checkoutDto.shippingAddress,  city: checkoutDto.city,  postalCode: checkoutDto.postalCode,  country: checkoutDto.country,  paymentMethod: checkoutDto.paymentMethod,  totalAmount: totalAmount.toString(),  status: 'pending',});