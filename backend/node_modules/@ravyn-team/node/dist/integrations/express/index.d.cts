import { ErrorRequestHandler } from 'express';
import { S as Sdk } from '../../sdk-Do1IS9bi.cjs';
import { I as Integration } from '../../integration-E_kvdpZ3.cjs';
import 'winston-transport';

declare class ExpressIntegration implements Integration {
    readonly name = "express";
    private sdk;
    setup(sdk: Sdk): void;
    teardown(): void;
    errorHandler(): ErrorRequestHandler;
}

export { ExpressIntegration };
